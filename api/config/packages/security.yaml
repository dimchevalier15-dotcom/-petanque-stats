security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        api:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            jwt: ~
        main:
            lazy: true
            security: false

    access_control:
        - { path: '^/api/live-matches/[0-9a-f-]{36}$', roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/config$, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/live-matches, roles: ROLE_USER }
        - { path: ^/api/matches, roles: ROLE_USER }
        - { path: ^/api/players, roles: ROLE_USER }
        - { path: ^/api/competitions, roles: ROLE_USER }
        - { path: ^/api/countries, roles: ROLE_USER }
        - { path: ^/api/clubs$, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/api/clubs, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
