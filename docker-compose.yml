version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: petanque_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: petanque
      MYSQL_USER: petanque
      MYSQL_PASSWORD: petanque
    ports:
      - "3310:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-ppetanque"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./api
    container_name: petanque_api
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      DATABASE_URL: "mysql://petanque:petanque@mysql:3306/petanque?serverVersion=8.0&charset=utf8mb4"
    ports:
      - "8080:8000"

  mobile:
    build:
      context: ./mobile
      dockerfile: Dockerfile
    container_name: petanque_mobile
    environment:
      NODE_ENV: development
    ports:
      - "5173:5173"
    volumes:
      - ./mobile:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

volumes:
  mysql_data: