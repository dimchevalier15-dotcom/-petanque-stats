# Mobile / Vite environment (copy to `.env.local` for local overrides).

# API base URL used by Axios.
# - `/api` (default): same host, nginx or Vite dev proxy forwards to the API.
# - `https://api.example.com`: API on a separate host (set CORS on the API).
VITE_API_URL=/api
