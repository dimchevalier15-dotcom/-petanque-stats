# Public API base URL consumed by Vite (VITE_* is inlined at build time).
# Leave empty in local development to use the Vite `/api` proxy (Docker API).
# Production web and Android: set the production API origin, including `/api`.
# Example: https://api.petanque-analytics.com/api
VITE_API_URL=
